const styles = (theme) => ({
	root: {
		color: theme.palette.common.gray,
		fontSize: '16px'
	},
	selected: {
		color: theme.palette.primary.main
	},
	shortDate: {
		fontSize:'12px',
		marginBottom: '5px'
	}
});

export default styles;